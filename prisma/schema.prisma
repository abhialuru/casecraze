 

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
 
model Configuration{
  sessionId String @id @default(uuid())
  width Int
  height Int
  imageUrl String
  croppedImageUrl String?
}